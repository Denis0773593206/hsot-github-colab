let fontSizeDropdown = document. getElementById('font -size');
let heading = document .getElementById('heading');

fontSizeDropdown.addEventListener("change", function()){
    console.log(fontSizeDropdown.valve)
    let fontSize = fontSizeDropdown.value;
}

let background color Dropdown = document. getElementById('backgound color');
let heading = document .getElementById('heading');

background colorDropdown.addEventListener("change", function()){
    console.log(backgroundColorDropdown.valve)
    let background color = backgroundColorDropdown.value;
}